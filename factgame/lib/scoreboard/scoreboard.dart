import 'package:flutter/material.dart';

class Scoreboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          //TODO: Return playername and score
          child: Text('Playername'),
        )
      ],
    );
  }
}